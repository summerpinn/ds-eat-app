import { VisitTimeAgoPipe } from './visit-time-ago.pipe';

describe('VisitTimeAgoPipe', () => {
  it('create an instance', () => {
    const pipe = new VisitTimeAgoPipe();
    expect(pipe).toBeTruthy();
  });
});
