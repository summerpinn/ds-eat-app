import { ShopClassUniquePipe } from './shop-class-unique.pipe';

describe('ShopClassUniquePipe', () => {
  it('create an instance', () => {
    const pipe = new ShopClassUniquePipe();
    expect(pipe).toBeTruthy();
  });
});
